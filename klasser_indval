# Klasser
# En klass är en struktur som består av objekt
# Med klassn Elever är det alltså att ett objekt i klassen är en elev


class Elever: # Man brukar döpa klasser med stor bokstav

    def __init__(self, namn, ålder, program): # en metod (som funktion fast för en klass)
        # just __init__ är nästan alltid med som metod och är för att skapa objektet
        # här skapas objektets attribut, till exempel namn
        # self måste alltid vara med som argument i alla metoder

        self.namn = namn
        self.ålder = ålder
        self.program = program

    
    def __str__(self): # __str__ är en annan metod som ofta är med, den avgör hur ett objekt skrivs ut
        beskrivning = self.namn + " " + self.program # här skapas en beskrivning som är vad vi vill ska skrivas ut
        return beskrivning # det som ska skrivas ut returneras
    
    def ålder_om_fem_år(self): # ett exempel på annan metod som beräknar åldern om fem år
        return self.ålder + 5


elev1 = Elever("Johan",17,"Ekonomi") # här skapar vi ett objekt med ett namn, en ålder och ett program
                                    # Detta sker med hjälp av __init__

print(elev1.namn) # här skrivs vår skapade elevs namn ut

print(elev1) # här skrivs elev1 ut, detta sker med hjälp av __str__

print(elev1.ålder_om_fem_år()) # här kör vi metoden ålder_om_fem_år, och skriver ut resultatet